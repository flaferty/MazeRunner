plugins {
    // Add this line so Gradle can download Java 17 automatically
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'MazeRunner'
include 'desktop', 'core'